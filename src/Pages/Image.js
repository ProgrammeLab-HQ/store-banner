import React from 'react'
export default function Image(props) {
    
    return (
        <>
            <button type='button' onClick={runUploader}>
                Open Uploader
            </button>
        </>
    )
}